# nginx configuration
# sudo apt-get update &&  sudo apt-get upgrade
# sudo apt-get install nginx -y 
#  systemctl status nginx 
# /var/www/html default path
# sudo vi /etc/nginx/sites-available/default


# sudo systemctl reload nginx

# netstat -lntp

# tcp6       0      0 :::8080                 :::*                    LISTEN      -

# http://localhost:8080/


# sudo apt update
# sudo apt install libnss3-tools -y

# curl -L https://github.com/FiloSottile/mkcert/releases/latest/download/mkcert-v1.4.4-linux-amd64 -o mkcert
# chmod +x mkcert
# sudo mv mkcert /usr/local/bin/

# mkcert -version

# mkdir -p ~/certs
# cd ~/certs



# mkcert linux-box.com localhost 127.0.0.1



# "sudo vi /etc/nginx/sites-available/default"


# /home/xor/linux-box/Linux-box/nginx/cert/linux-box.com+2-key.pem

# /home/xor/linux-box/Linux-box/nginx/cert/linux-box.com+2.pem



# curl -vk https://localhost:443


# C:\Windows\System32\drivers\etc
# need to modify host file

# 127.0.0.1   linux-box.com
